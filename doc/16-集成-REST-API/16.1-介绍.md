# 16.1 介绍

## 介绍 (Introduction)

Frappe 内置了一个 HTTP API，可以分为远程过程调用（RPC）和表现层状态转移（REST）两类：
- RPC 用于调用白名单中的方法
- REST 用于操作资源

基础 URL 是 `https://{你的 frappe 实例}`。这里展示的每个请求都应该添加到你的基础 URL 之后。例如，如果你的实例是 `demo.erpnext.com`，`GET /api/resource/User` 意味着 `GET https://demo.erpnext.com/api/resource/User`。