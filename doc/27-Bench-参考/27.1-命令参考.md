# 27.1 命令参考

## 📋 Bench 命令参考文档

### 🏠 **站点管理命令**
1. **`bench new-site`**  
   - 创建新站点。
2. **`bench drop-site`**  
   - 删除指定站点。
3. **`bench migrate`**  
   - 执行数据库迁移（更新站点结构）。
4. **`bench backup`**  
   - 备份指定站点。
5. **`bench reinstall`**  
   - 重新安装站点（清空数据并重新初始化）。
6. **`bench list-apps`**  
   - 列出当前站点安装的所有应用。
7. **`bench install-app`**  
   - 安装指定应用到站点。
8. **`bench uninstall-app`**  
   - 从站点卸载指定应用。
9. **`bench show-config`**  
   - 显示当前站点的配置信息。
10. **`bench set-config`**  
    - 设置或修改站点配置项。
11. **`bench restore`**  
    - 从备份文件恢复站点。
12. **`bench partial-restore`**  
    - 部分恢复站点数据（可选表恢复）。

---

### ⚙️ **Frappe 工具命令**
1. **`bench version`**  
   - 显示当前安装的Frappe及Bench版本信息。

---

### 🗃️ **数据库维护命令**
1. **`bench transform-database`**  
   - 转换数据库结构（如字符集、表引擎等）。
2. **`bench trim-database`**  
   - 清理数据库冗余数据（如旧日志、临时表）。
3. **`bench trim-tables`**  
   - 针对特定表进行数据清理。

---

### 🌐 **翻译管理命令**
1. **`bench generate-pot-file`**  
   - 生成PO翻译模板文件（.pot）。
2. **`bench migrate-csv-to-po`**  
   - 将CSV格式的翻译迁移到PO文件。
3. **`bench update-po-files`**  
   - 更新PO文件以同步最新翻译词条。
4. **`bench compile-po-to-mo`**  
   - 将PO文件编译为MO二进制文件（供系统使用）。

---

### 📌 使用说明
- 所有命令均在 **Bench CLI** 环境下运行。
- 部分命令需指定站点名称（如 `bench backup --site sitename`）。
- 详细参数可通过 `bench [command] --help` 查看。

**相关链接**: https://docs.frappe.io/framework/user/en/bench/reference